# OS / editor
.DS_Store
Thumbs.db
*.swp

# fast-feedback runtime output (generated when the tool is used)
fast-feedback.snippet.js
fast-feedback.bookmarklet.txt
feedback-screenshot.png

# feedback spool + history archive (inbox jsonl/md, pending items, history PNGs) —
# local-only runtime data; screenshots may hold sensitive UI, never commit it
.ffb/

# project-scoped settings written by the overlay
.claude/skills/fast-feedback/settings.json

# Agent Bus working artifacts (orchestration state, local-only)
/agent-bus/

# Local-only docs (plans / registry) — kept private, not published
/docs/
